<div class="filterService filters">
    <div class="filters-block">
        <div class="input-group">
            <div class="input-group-prepend">
                <div class="input-group-text circle">Du</div>
            </div>
            <input type="date" class="form-control circle" name="dateMin" id="dateMin" placeholder="Date de début">
            <div class="input-group-prepend">
                <div class="input-group-text">Au</div>
            </div>
            <input type="date" class="form-control circle" name="dateMax" id="dateMax" placeholder="Date de fin">
        </div>
    </div>

    <div class="filters-block">
        <div class="input-group">
            <select class="form-control data circle" name="statut" id="statutFilter">
                <option value>Statut</option>
                {% for statut in statuts %}
                    <option value="{{ statut.label }}">{{ statut.label }}</option>
                {% endfor %}
            </select>
        </div>
    </div>

    <div>
        <select name="Demandeur" id="demandeurFilter" class="form-control data select2">
            <option></option>
            {% for utilisateur in users %}
                <option value="{{ utilisateur.name }}">
                    {{ utilisateur.name }}
                </option>
            {% endfor %}
        </select>
    </div>
    <div>
        <select name="Validateur" id="validateurFilter" class="form-control data select2">
            <option></option>
            {% for utilisateur in users %}
                <option value="{{ utilisateur.name }}">
                    {{ utilisateur.name }}
                </option>
            {% endfor %}
        </select>
    </div>

    <div>
        <div id='submitFilterHolidays' class="btn bg-grey min-105px" tabindex="0">
            <i class="fas fa-filter mr-2"></i>
            Filtrer
        </div>
    </div>
</div>
